apiVersion: v1 # for versions before 1.9.0 use apps/v1beta2
kind: Pod
metadata:
  name: nginx
  namespace: default
  labels: 
    app: web
spec:
  containers:
  - name: nginx
    image: nginx:1.7.9
    ports:
        - containerPort: 80
